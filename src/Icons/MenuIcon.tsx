import MuiMenuIcon from "@mui/icons-material/Menu";
import IconProps from "./IconProps";

function MenuIcon({ color }: IconProps) {
  return <MuiMenuIcon color={color} />;
}

export default MenuIcon;
