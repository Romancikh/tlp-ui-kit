import MuiEditIcon from "@mui/icons-material/Edit";
import IconProps from "./IconProps";

function EditIcon({ color }: IconProps) {
  return <MuiEditIcon color={color} />;
}

export default EditIcon;
